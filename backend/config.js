// config.js
const config = {
    kaliIP: "REPLACE_WITH_KALI_IP", // Replace with your Kali VM's IP address
    sshUser: "REPLACE_WITH_USERNAME",         // SSH username
    sshPassword: "REPLACE_WITH_PASSWORD",     // SSH password
    nmapQuickCommand: "REPLACE_WITH_NMAP_COMMAND",
    nmapAdvancedCommand: "REPLACE_WITH_NMAP_COMMAND"
  };
  
  module.exports = config;
  